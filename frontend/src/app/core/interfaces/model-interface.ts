
export interface ModelResponse {
    id: number;
    model: string;
    provider: string;
    createdAt?: string;
    updatedAt?: string;
}

export interface ModelConfig {
    id: number;
    model: string;
    provider: string;
    createdAt?: string;
    updatedAt?: string;
}
